### VersionIntervalSet

The `VersionIntervalSet` unit is documented in `version-interval.md`, which covers both
`VersionInterval` and `VersionIntervalSet` comprehensively, including the data model, parsing
algorithm, `Contains` overloads, and interaction table.
